import { Routes } from '@angular/router';
import { SignalsPlaygroundComponent } from './signals-playground.component';

export const signalsPlaygroundRoutes: Routes = [
  { path: '', component: SignalsPlaygroundComponent },
];
